---
/**
 * Button — Vercel-style pill button. Renders an <a> when `href` is given,
 * otherwise a <button>.
 */
interface Props {
  href?: string;
  variant?: "primary" | "secondary" | "ghost";
  size?: "md" | "lg";
  type?: "button" | "submit" | "reset";
  class?: string;
  [key: string]: any;
}

const {
  href,
  variant = "primary",
  size = "md",
  type = "button",
  class: className = "",
  ...rest
} = Astro.props;

const base =
  "inline-flex items-center justify-center gap-2 font-medium rounded-md transition-all duration-150 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand disabled:opacity-50 disabled:pointer-events-none whitespace-nowrap";

const variants = {
  primary:
    "bg-brand text-white hover:bg-brand-hover shadow-sm",
  secondary:
    "bg-surface text-ink border border-hairline-strong hover:border-ink hover:bg-canvas-soft",
  ghost: "text-body hover:text-ink hover:bg-canvas-soft",
};

const sizes = {
  md: "h-9 px-4 text-sm",
  lg: "h-11 px-6 text-base",
};

const cls = `${base} ${variants[variant]} ${sizes[size]} ${className}`;
---

{
  href ? (
    <a href={href} class={cls} {...rest}>
      <slot />
    </a>
  ) : (
    <button type={type} class={cls} {...rest}>
      <slot />
    </button>
  )
}
